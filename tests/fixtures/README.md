# Test fixtures

Captured 17track v2.4 responses go here in M1 (seeded from the real session
on 2026-07-03):

- Correios (carrier 2151)
- YanWen (carrier 190012)
- An `Exception` / `Exception_Returning` payload
- A mixed `accepted` + `rejected` response
- An already-registered rejection (`-18019901`)
- A recorded raw webhook body + its real `sign` header (byte-exact)

Empty until M1.
